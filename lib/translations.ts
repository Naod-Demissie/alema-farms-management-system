/**
 * Translation utilities for dynamic content and enums
 */

/**
 * Get translation key for expense category enum
 */
export function getExpenseCategoryKey(category: string): string {
  return `enums.expenseCategory.${category}`;
}

/**
 * Get translation key for revenue source enum
 */
export function getRevenueSourceKey(source: string): string {
  return `enums.revenueSource.${source}`;
}

/**
 * Get translation key for feed type enum
 */
export function getFeedTypeKey(feedType: string): string {
  return `enums.feedType.${feedType}`;
}

/**
 * Get translation key for bank name enum
 */
export function getBankNameKey(bankName: string): string {
  return `enums.bankName.${bankName}`;
}

/**
 * Get translation key for attendance status enum
 */
export function getAttendanceStatusKey(status: string): string {
  return `enums.attendanceStatus.${status}`;
}

/**
 * Get translation key for leave type enum
 */
export function getLeaveTypeKey(leaveType: string): string {
  return `enums.leaveType.${leaveType}`;
}

/**
 * Get translation key for leave status enum
 */
export function getLeaveStatusKey(status: string): string {
  return `enums.leaveStatus.${status}`;
}

/**
 * Get translation key for treatment response enum
 */
export function getTreatmentResponseKey(response: string): string {
  return `enums.treatmentResponse.${response}`;
}

/**
 * Get translation key for disease class enum
 */
export function getDiseaseClassKey(diseaseClass: string): string {
  return `enums.diseaseClass.${diseaseClass}`;
}

/**
 * Get translation key for death cause enum
 */
export function getDeathCauseKey(cause: string): string {
  return `enums.deathCause.${cause}`;
}

/**
 * Get translation key for feed unit enum
 */
export function getFeedUnitKey(unit: string): string {
  return `enums.feedUnit.${unit}`;
}

/**
 * Get translation key for inventory type enum
 */
export function getInventoryTypeKey(type: string): string {
  return `enums.inventoryType.${type}`;
}

